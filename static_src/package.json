{
  "name": "@stapel/attributes-admin",
  "version": "0.0.0",
  "private": true,
  "description": "Schema-driven admin UI (Lit 3 web components) for stapel-attributes. Source; the built ESM bundle is committed under ../static/stapel_attributes/.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
