{
  "expect": {
    "condition": {
      "forbidden_options": [],
      "max": null,
      "min": null,
      "required": false,
      "visible": true
    },
    "screen_state": {
      "forbidden_options": [],
      "max": null,
      "min": null,
      "required": false,
      "visible": true
    }
  },
  "features": [
    {
      "config": {
        "maxSelected": 1,
        "options": [
          {
            "label": "new",
            "value": "new"
          },
          {
            "label": "used",
            "value": "used"
          }
        ],
        "type": "select"
      },
      "slug": "condition"
    },
    {
      "config": {
        "max": 100,
        "min": 0,
        "type": "int"
      },
      "mandatory": false,
      "rules": [
        {
          "effect": "limit",
          "max": 10,
          "when": {
            "all": [
              {
                "feature": "condition",
                "op": "not_in",
                "values": [
                  "new"
                ]
              }
            ]
          }
        }
      ],
      "slug": "screen_state"
    }
  ],
  "id": "limit-not-in-on-unanswered-controller",
  "note": "an unmatched `limit` narrows nothing while the controller is unanswered",
  "values": {}
}
