{
  "expect": {
    "condition": {
      "forbidden_options": [],
      "max": null,
      "min": null,
      "required": false,
      "visible": true
    },
    "section": {
      "forbidden_options": [],
      "max": null,
      "min": null,
      "required": false,
      "visible": true
    }
  },
  "features": [
    {
      "config": {
        "maxSelected": 1,
        "options": [
          {
            "label": "new",
            "value": "new"
          },
          {
            "label": "used",
            "value": "used"
          }
        ],
        "type": "select"
      },
      "slug": "condition"
    },
    {
      "config": {
        "type": "header"
      },
      "mandatory": true,
      "rules": [
        {
          "effect": "hide",
          "when": {
            "all": [
              {
                "feature": "condition",
                "op": "in",
                "values": [
                  "new"
                ]
              }
            ]
          }
        }
      ],
      "slug": "section"
    }
  ],
  "id": "header-always-visible",
  "note": "a header is visible and never required, whatever the rules say",
  "values": {
    "condition": [
      "new"
    ]
  }
}
