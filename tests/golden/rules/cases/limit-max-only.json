{
  "expect": {
    "condition": {
      "forbidden_options": [],
      "max": null,
      "min": null,
      "required": false,
      "visible": true
    },
    "weight": {
      "forbidden_options": [],
      "max": 50,
      "min": null,
      "required": false,
      "visible": true
    }
  },
  "features": [
    {
      "config": {
        "maxSelected": 1,
        "options": [
          {
            "label": "new",
            "value": "new"
          },
          {
            "label": "used",
            "value": "used"
          }
        ],
        "type": "select"
      },
      "slug": "condition"
    },
    {
      "config": {
        "max": 100,
        "min": 1,
        "type": "int"
      },
      "mandatory": false,
      "rules": [
        {
          "effect": "limit",
          "max": 50,
          "when": {
            "all": [
              {
                "feature": "condition",
                "op": "in",
                "values": [
                  "used"
                ]
              }
            ]
          }
        }
      ],
      "slug": "weight"
    }
  ],
  "id": "limit-max-only",
  "note": "a limit rule may carry only max",
  "values": {
    "condition": [
      "used"
    ]
  }
}
